# Implementation Plans

Generated by the improve skill on 2026-09-13, against commit `e64f976`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make study-material generation cancellation actually stop the stream | P1 | M | — | DONE |
| 002 | Recover job-queue rows stuck in `processing`; drain on shutdown | P1 | M | — | TODO |
| 003 | Fail closed when `DEV_STORAGE_TOKEN_SECRET` is unset | P1 | S | — | TODO |
| 004 | Bound ZIP decompression before archive-bomb checks | P1 | M | — | TODO |
| 005 | Fetch only recent chat history instead of the whole transcript | P1 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

All five plans are independent — none blocks another. If you want quick wins
first, run 003 and 005 before 001, 002, and 004.

## Dependency notes

- None. Each plan touches a disjoint area (study-materials generation, jobs,
  storage, zip parsing, chat history) and can land in any order.
- 001 and 002 both concern long-running backend work but share no files;
  running them back-to-back is fine.

## Findings considered and rejected

These were audited and deliberately excluded, so nobody re-audits them:

- **Missing authentication / authorization / tenant ownership checks**:
  deliberately removed in commit `c3d8935` (`feat!: remove authentication and
  per-user scoping`). By design for a single-user local app.
- **SQL injection, command injection, XSS, path traversal**: checked and
  unreachable. No request-derived SQL/`exec`/`eval`; markdown renders with raw
  HTML disabled and `rehype-sanitize`; `safeJoin` prefix-checks resolved paths.
- **`image-size` high advisory via `pptxgenjs`**: the vulnerable image-parser
  path is never called by `slides-builder.service.ts`.
- **Dead always-true branch** at `sources.service.ts:391,507`
  (`if (this.sourceJobsService.enqueueProcessing)`): harmless; delete only if
  that file is touched for another reason.
- **Route code-splitting / shiki chunking / model-catalog caching**: verified
  working as intended.
- **God files** (`slides-preview.ts` 1430 lines, `content-normalizer.ts` 954,
  `document-normalizer.service.ts` 890, `slides-design-resolver.ts` 857):
  real tech debt, but an L-effort refactor. Do it only after characterization
  tests exist for the slide resolver/renderer.
- **`listMessages` unbounded transcript** (`chat.service.ts:104`): folded into
  plan 005 as a deferred follow-up; the hot path fixed there is
  `getRecentHistory`.

## Lower-priority findings not yet planned

These were vetted and are available if requested: composite indexes for list
queries; reindex/reembed N+1 + `rawText` over-fetch; speaker-label N+1;
SSRF DNS-rebinding + robots redirect; `0.0.0.0` bind vs loopback log;
raw internal error strings returned to clients; missing security headers;
untested NDJSON streaming contract; tests that hand-roll schema instead of
running migrations; no CI + backend lint glob misses `tests/`; README links to
a nonexistent `docs/`; dead duplicate components and unused deps; prompt
injection data boundary; passphrase KDF fallback; and the `ai` v7/v6 SDK skew.
Ask the improve skill to turn any of them into plans.
